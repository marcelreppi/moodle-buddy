# Project agent instructions

- Do not add explicit Vue component `name` blocks in Nuxt components unless there is a concrete need. Nuxt already infers component names from file paths.
- Do not put key-value parameters in URL fragments. Use regular query parameters unless the user explicitly asks otherwise.
